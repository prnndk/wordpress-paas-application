[mysqld]
server-id = 1
log_bin = mysql-bin
binlog_format = ROW
binlog_do_db = wp_paas
gtid_mode = ON
enforce_gtid_consistency = ON

# Performance tuning - reduced for low memory servers
innodb_buffer_pool_size = 128M
innodb_log_file_size = 32M
innodb_flush_log_at_trx_commit = 1
sync_binlog = 1

# Character set
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

# Connection settings - reduced to save memory
max_connections = 200
wait_timeout = 28800
interactive_timeout = 28800

# Slow query log
slow_query_log = 1
slow_query_log_file = /var/log/mysql/mysql-slow.log
long_query_time = 2
