# WordPress PaaS Environment Variables
# Copy this file to .env and fill in the values

# Domain Configuration
DOMAIN=localhost
ACME_EMAIL=admin@example.com

# MySQL Configuration
MYSQL_ROOT_PASSWORD=change-me-root-password
MYSQL_REPLICATION_PASSWORD=change-me-replication-password
MYSQL_APP_PASSWORD=change-me-app-password

# JWT Secret for Auth
JWT_SECRET=change-me-super-secret-jwt-key-at-least-32-chars

# Traefik Dashboard Auth (generate with: htpasswd -nb admin yourpassword)
TRAEFIK_DASHBOARD_AUTH=admin:$apr1$rELwIMDe$cJZBmH9oAqDl22VGglaDp.

# GlusterFS Worker IPs
GLUSTER_WORKER1_IP=10.0.0.11
GLUSTER_WORKER2_IP=10.0.0.12

# Swarm Manager IP
SWARM_MANAGER_IP=10.0.0.10
