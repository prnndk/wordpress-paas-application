# Dashboard Environment Variables

# API URL for the orchestrator backend
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Server IP for path-based WordPress URLs (e.g., http://IP/slug)
# This should be the IP address of your Docker Swarm cluster
NEXT_PUBLIC_SERVER_IP=localhost

# Domain (alternative to SERVER_IP if using domain-based routing)
NEXT_PUBLIC_DOMAIN=localhost
